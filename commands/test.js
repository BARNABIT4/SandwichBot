exports.run = function(msg) {
	msg.delete()
	msg.channel.send('looking to install NTRcfw? Go here: https://github.com/Nanquitas/BootNTR/releases Banner doesnt matter, Mode3 is only for extended memory games on the O3DS systems').then(msg => {
	})
}